for i, v in enumerate(tournaments.values):
    # Your code here
    # i is the index or count of the current iteration
    # v is the value at that index in tournaments.values
    # You can perform operations using i and v within this loop
